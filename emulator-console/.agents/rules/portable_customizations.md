# Portable Workspace Customizations

- Never use absolute user paths (such as `/home/<username>/...`) in `.agents/` rules, skills, or workspace documentation.
- Always use workspace-relative paths (e.g., `./.clang-format`, `main/`).
